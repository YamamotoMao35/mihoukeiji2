.registration-page
  .registration-container.col-sm-8.col-md-8
    #contact-form.container
      - breadcrumb :registration
      = render 'layouts/breadcrumbs'
      %h3.mt-4.col-sm-5.col-md-5 新規登録画面
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        = render "devise/shared/error_messages", resource: resource
        .form-group
          = f.label "名前"
          %span.form-require 必須
          %br/
          = f.text_field :name, autofocus: true, autocomplete: "name", placeholder: "例)みほ太郎", class: "form-control required"
        .form-group
          = f.label "メールアドレス"
          %span.form-require 必須
          %br/
          = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "PC・携帯どちらでも可", class: "form-control required"
        .form-group
          = f.label "パスワード"
          %span.form-require 必須
          %br/
          = f.password_field :password, autocomplete: "new-password", placeholder: "6文字以上", class: "form-control required"
        .form-group
          = f.label "パスワード(確認用)"
          %span.form-require 必須
          %br/
          = f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "6文字以上", class: "form-control required"
        .registration-notice
          「会員登録」のボタンを押すことにより
          %span= link_to "利用規約", attention_term_path
          、
          %span= link_to "プライバシーポリシー", attention_privacy_policy_path
          に同意したものとみなします。
        .actions.d-flex
          = f.submit "会員登録", id: "submit-form", class: "btn btn-secondary btn-lg"
      %h4.notice ※すでに登録がお済みの場合
      = link_to "ログインはこちら", new_user_session_path, class: "btn btn-login-link"
